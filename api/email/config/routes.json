{
  "routes": [
    {
      "method": "POST",
      "path": "/email-send",
      "handler": "email.send",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/emails",
      "handler": "email.create",
      "config": {
        "policies": []
      }
    }
  ]
}
